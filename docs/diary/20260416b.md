Hôm qua chúng ta đang làm việc với zen trong việc lấy từng thành phần của Gun và Sea rồi port từng phần sang Zen. Nhưng tôi 1 agent khác báo cáo rằng các thành phần của Gun chưa thực sự được port đúng nghĩa, thay vào đó zen đã bọc lấy gun (giống như copy nguyên code gun rồi wrap). Hãy kiểm tra để xác nhận lại một lần nữa. Nếu điều đó là sự thật, chúng ta cần fix.

Fix các file trong lib/ và examples/ để chuyển qua zen

Fix toàn bộ các file trong test/ để đảm bảo zen có bộ unit test toàn vẹn nhất 

Không được tự ý commit / push

Sau khi fix phải chạy npm test