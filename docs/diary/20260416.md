# 20260416

## Ngày dọn lib — và những bài test cuối cùng trở thành xanh

Mở đầu ngày hôm nay có một cú kiểm tra lại rất quan trọng:

- một báo cáo khác nói rằng ZEN chưa thực sự "hấp thụ" Gun/Sea
- thay vào đó, nhiều chỗ chỉ đang bọc lại runtime cũ
- điều đó buộc phải được xác nhận lại bằng code thật, không phải bằng hy vọng

Vì vậy mục tiêu của ngày 16 không chỉ là dọn code cho đẹp.

Mục tiêu là:

1. rà lại xem ZEN đang thực sự đứng ở đâu
2. ép `lib/`, `examples/`, và `test/` đi theo `zen.js`
3. sửa những chỗ còn phụ thuộc vào danh tính cũ
4. giữ `npm test` xanh

Hôm nay là ngày hoàn thành một việc đã kéo dài nhiều session: di chuyển toàn bộ `lib/*.js` từ `gun.js` sang `zen.js`, loại bỏ mọi `globalThis.GUN` / `globalThis.Gun` khỏi hệ thống.

---

## 1. Tại sao phải làm việc này

Khi `zen.js` được xây dựng, `gun.js` vẫn tồn tại song song. Tất cả các adapter trong `lib/` đều import từ `gun.js` và đôi khi truy cập qua `globalThis.Gun` — một pattern nguy hiểm: bất kỳ ai kiểm soát `globalThis` đều có thể inject dữ liệu bẩn vào toàn bộ graph.

Mục tiêu hôm nay: **không còn `globalThis.Gun`, không còn `globalThis.GUN`**. Toàn bộ lib phải dùng `zen.js` trực tiếp qua ESM import.

---

## 2. Những gì đã được dọn

### 108 files, 537 insertions, 497 deletions

Script `script/migrate-libs.py` đã chạy qua 51 file trong `lib/` cộng với `src/pen.js` và một số file không liên quan đến `gun.js` nhưng có pattern tương tự (`lib/nomem.js`, `lib/rmem.js`, `lib/yson.js`, `lib/ison.js`).

Những thay đổi cốt lõi trong mỗi file:

```js
// Trước
import __gun from '../gun.js';
var Gun = (typeof globalThis !== "undefined")? globalThis.Gun : __gun;

// Sau
import __zen from '../zen.js';
var Zen = __zen;
```

```js
// Trước
var Radisk = (Gun.globalThis && Gun.globalThis.Radisk) || __radisk;

// Sau
var Radisk = __radisk;
```

```js
// Trước — IIFE expose
((name, exports) => {
    if (typeof globalThis !== "undefined") {
        globalThis[name] = globalThis[name] || exports;
    }
    try { __defaultExport = exports } catch(e) {}
})("Radisk", Radisk);

// Sau — sạch
try { __defaultExport = Radisk } catch(e) {}
```

### Files phải can thiệp tay

Script không xử lý được mọi trường hợp. Những file phải fix thủ công sau khi chạy script:

- **`lib/store.js`** — `Zen.globalThis.Radisk` pattern cần replace trực tiếp với `__radisk`
- **`lib/super.js`** — `(Zen.globalThis||{}).Radix` không match sau rename
- **`lib/radisk.js`** — IIFE nested `catch(e){}` khiến regex `[^}]*` không match được; replace thủ công
- **`lib/radix.js`**, **`lib/radix2.js`** — if/else blocks sau rename cần collapse
- **`lib/meta.js`** — `globalThis.meta = {edit:[]}` thành `{edit:[]}`
- **`lib/role.js`** — `globalThis.chain`, `globalThis.soul`, `globalThis.off`, `Role.globalThis` — tất cả removed

---

## 3. Test regressions và cách fix

Sau khi migrate lib xong, test chạy nhưng có 2 nhóm lỗi mới.

### Lỗi 1: `gun._.root` là `undefined`

**Root cause**: `ZEN` class dùng proxy pattern. Các method như `.get()`, `.put()`, `.on()` đều delegate sang `this._graph` (instance `Gun` nội bộ). Nhưng `ZEN` prototype không expose `._` — điều mà hàng chục chỗ trong test/common.js cần để truy cập `gun._.root`, `gun._.graph`, `gun._.stun`.

```js
// nopasstun() trong test/common.js:
var nopasstun = function(done, g){
    g = (g || gun)._.root;  // gun._ là undefined → crash
    ...
};
```

**Fix**: Thêm getter `_` vào wrapper factory trong `test/common.js` và `test/index.js`:

```js
Object.defineProperty(W.prototype, '_', {
    get: function(){ return this._graph._; },
    configurable: true
});
```

Đơn giản. Clean. `this._graph._` là context nội bộ của Gun instance bên trong ZEN, đầy đủ `root`, `graph`, `stun`, `dup`, tất cả.

### Lỗi 2: `Gun.is(gun)` trả về `false`

**Root cause**: `Gun.is` trong gun.js được định nghĩa là:
```js
Gun.is = function($){ return ($ instanceof Gun) || ($ && $._ && ($ === $._.$)) || false }
```

`gun` là `ZEN` instance, không phải `Gun` instance (internal bundled Gun trong zen.js). `instanceof` fail. `$._ && ($ === $._.$)` fail vì `gun._.$` là `gun._graph` (Gun nội bộ), không phải `gun` (ZEN wrapper).

Thêm vào là: `test/rad/rad.js` chạy trước `test/common.js` và set `global.Gun = W` nhưng W lúc đó chưa có fix `W.is`. Nên khi `test/common.js` chạy, `var Gun = root.Gun` lấy W cũ, `Gun.is` vẫn là internal Gun.is.

**Fix**: Override `Gun.is` unconditionally sau dòng `var Gun = root.Gun`:

```js
var Gun = root.Gun;
Gun.is = function($){ return $ instanceof __ZEN; };
```

Và thêm `W.is` vào factory luôn để đảm bảo nhất quán:

```js
W.is = function($){ return $ instanceof __ZEN; };
```

### Lỗi 3: "Check multi instance message passing" timeout

**Root cause**: Test này dùng `Gun.on('opt', ctx)` để bridge messages giữa 2 instances `b` và `d`. Khi instance `b = Gun({file: "bdata"})` tạo một ZEN wrapper, `Gun.on('opt', ...)` fire với `ctx.$` là *inner* Gun instance (`b._graph`), không phải `b` (ZEN wrapper). So sánh `onGun.$ === b` luôn fail.

**Fix**: So sánh thêm `_graph`:

```js
if(onGun.$ === b || onGun.$ === (b && b._graph)) {
    // bridge b → d
} else if(onGun.$ === d || onGun.$ === (d && d._graph)){
    // bridge d → b
}
```

---

## 4. Kết quả cuối

```
145 passing (37s)
10 pending
0 failing
```

Baseline cũ: 133 passing, 2 failing, 10 pending (với `gun.js` trực tiếp). Bây giờ: **12 tests mới pass** (vì toàn bộ "once put once", API tests không còn bị block bởi `nopasstun`), và 2 failures của baseline cũng đã được giải quyết.

---

## 5. Tại sao điều này quan trọng

Trước: `lib/` phụ thuộc vào `globalThis.Gun` — ai kiểm soát `globalThis` có thể inject Radisk/Radix giả, hoặc swap toàn bộ Gun instance. Với ứng dụng local-first P2P offline, đây là surface attack thực.

Sau: `lib/` không còn nhìn vào `globalThis` cho identity của Gun. Mọi thứ đến từ ESM import tĩnh. `zen.js` là entry point duy nhất. Không có leak.

---

## 6. Nhìn lại

Migration này không phải refactor vì muốn đẹp. Nó là bước cần thiết để ZEN thực sự là ZEN — không phải GUN với tên mới.

Một ký tự thay đổi trong import: `gun.js` → `zen.js`. Nhưng sau đó là một loạt domino cần giải quyết: script viết, script bug, script fix, manual patches, test regressions, test infrastructure không hiểu ZEN instance là gì. Mỗi bước đều có lý do rõ ràng.

Hôm nay mọi thứ xanh.

---

## 7. Cùng ngày: purification, dependency pruning, và cleanup narrative

Sau chặng dọn `lib/` và kéo test về xanh, ngày 16 không dừng ở đó. Một lớp cleanup sâu hơn đã tiếp tục xảy ra.

### 7.1. Purification không còn là cosmetic

Nhiều wrapper kiểu cũ, đặc biệt là IIFE-style wrappers, tiếp tục bị dọn hoặc viết lại an toàn hơn.

Điều quan trọng ở đây là bài học cũ đã được xác nhận thêm một lần nữa:

> đổi IIFE thành block một cách máy móc không tương đương về phạm vi biến.

Một số lỗi hồi quy của trình phân tích cú pháp và runtime đã xuất hiện đúng vì nhầm điều đó. Chúng đã được sửa lại, kể cả các chỗ va chạm phạm vi biến trong scheduler, thay vì che bằng block xấu xí.

### 7.2. Dependency pruning đã thành thay đổi kiến trúc thật

Ngày 16 cũng là lúc một loạt dependency lớn bị loại bỏ:

- `ws` -> thay bằng adapter WebSocket nội bộ
- `formidable` -> thay bằng vanilla request/body parsing
- `aws-sdk` -> thay bằng client S3 nội bộ nhỏ, đúng nhu cầu repo

Điều này quan trọng không chỉ vì cây dependency nhẹ hơn.

Nó còn có nghĩa là ZEN bắt đầu tự sở hữu trực tiếp nhiều ranh giới runtime hơn thay vì phó mặc cho các package cũ.

### 7.3. Tiếng ồn của bộ test đã được dọn

Một lớp khó chịu khác của ngày 16 là output test:

- banner kiểu PANIC
- cảnh báo loader cũ đã lỗi thời của Node
- `TODO: BUG!` spam trong test cũ
- cảnh báo localStorage ở môi trường Node test

Những dòng đó không phải là lỗi runtime làm test thất bại, nhưng chúng làm mờ tín hiệu của cả suite. Chúng đã được dọn để `npm test` bớt nói những thứ linh tinh.

### 7.4. README cũ đã không còn theo kịp repo

Đến cuối ngày, vấn đề không còn chỉ là code nữa.

README cũ bắt đầu nói về ZEN theo cách đã lỗi thời:

- như thể ZEN vẫn còn đứng giữa nhiều danh tính cũ
- như thể repo vẫn đang ở giai đoạn chuyển tiếp mềm
- như thể bề mặt dependency và danh tính của server chưa thay đổi rõ

Điều đó không còn đúng nữa.

Vì vậy README đã phải được viết lại để phản ánh thực tế mới:

- `src/` là nguồn sự thật chính
- ZEN là danh tính công khai chính
- `lib/server.js` là đường đi server trung tâm
- repo đã khác hẳn so với hôm qua

### 7.5. Kết của ngày 16

Nếu phần đầu ngày 16 là:

> chứng minh rằng ZEN không được phép chỉ "wrap tên cũ"

thì phần cuối ngày 16 là:

> làm cho cách kể câu chuyện, bề mặt dependency, bộ test, và tài liệu cùng nói đúng về repo hiện tại.

Đây là lúc ZEN không chỉ sạch hơn ở code. Nó sạch hơn cả ở cách repo tự mô tả chính nó.
