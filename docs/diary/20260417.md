# 20260417

## Ngày của Windows, đường dẫn cũ, và một lần format toàn repo

Hôm nay không phải kiểu ngày làm feature mới rồi chụp ảnh đẹp.

Hôm nay là kiểu ngày phải bò qua từng lớp bug để đưa repo về trạng thái ổn định sau một loạt thay đổi lớn.

Có 3 việc chính chồng lên nhau:

1. sửa các vấn đề tương thích, đặc biệt là trên Windows
2. di chuyển toàn bộ `src/zen/*` ra `src/*`
3. chạy format code toàn diện bằng Prettier mà không làm vỡ build hay test

Điểm tốt là cuối ngày, mọi thứ đều quy về một kết quả rõ ràng:

- build chạy lại được
- test xanh hết
- codebase gọn hơn lúc bắt đầu

---

## 1. Hôm nay thực sự là ngày fix bug để sống sót trên Windows

Có những ngày bug nằm ở business logic. Hôm nay bug nằm ở những thứ nhỏ, dai, và rất tốn sức: đường dẫn, shell behavior, môi trường runtime, và các assumption ngầm mà trước đó Unix chịu được còn Windows thì không.

Loại bug này mệt ở chỗ nó không tạo ra một lỗi duy nhất.

Nó tạo ra chuỗi triệu chứng:

- test treo ở những chỗ bất đồng bộ
- command chạy được ở một môi trường nhưng lệch ở môi trường khác
- file/path đổi vị trí là build đổ dây chuyền
- formatter hoặc tooling đụng vào code cũ thì lộ ra thêm lỗi parse legacy

Phần lớn thời gian hôm nay không phải để viết tính năng mới.

Phần lớn thời gian là để gỡ từng assumption cũ ra khỏi repo, để project cư xử ổn định hơn với Windows thay vì chỉ “may mắn chạy được”.

Đó là loại tiến triển ít hào nhoáng nhưng rất thật.

---

## 2. `src/zen/*` được dời ra `src/*` và build vỡ ngay lập tức

Đây là thay đổi cấu trúc đúng đắn, nhưng nó cũng bóc ngay những chỗ import cũ còn sót lại.

Sau khi flatten source tree, build không hỏng vì bundler “xấu”. Build hỏng vì trong code vẫn còn nhiều đường dẫn kiểu cũ trỏ đến:

- `./zen/shim.js`
- `../zen/shim.js`

Khi `src/zen/*` không còn nữa, những import này trở thành `ENOENT` rất nhanh và rất rõ.

Việc cần làm không phải là vá build script theo kiểu vòng vo. Việc đúng là sửa tận gốc các import stale trong source để chúng phản ánh đúng layout mới:

```js
// cũ
import shim from './zen/shim.js'

// mới
import shim from './shim.js'
```

và tương tự với các file trong `src/curves/` dùng `../shim.js`.

Sau khi dọn hết các tham chiếu cũ, `npm run build` quay lại trạng thái hoạt động bình thường.

Điểm quan trọng ở đây là: repo bây giờ không còn sống dựa trên layout cũ nữa. `src/*` đã trở thành layout thật, không phải chỉ là một lớp rename nửa vời.

---

## 3. Format toàn repo: không còn chỉ là `src/*.js`

Một thay đổi khác hôm nay là đưa formatting từ mức tượng trưng sang mức thực dụng.

Trước đó script format chỉ nhắm vào một tập rất hẹp. Mục tiêu hôm nay là rõ ràng hơn: nếu đã format thì format **toàn codebase**, không phải chỉ vài file trong `src/`.

Script `format` được đổi sang chạy Prettier trên toàn repo:

```json
"format": "prettier --write ."
```

Nhưng chạy Prettier toàn repo không đơn giản là đổi một dòng script rồi xong.

Ngay lập tức xuất hiện 2 lớp vấn đề:

### 3.1. `.prettierignore` cũ chặn gần như mọi thứ

Nó phải được thay bằng danh sách ignore có chủ đích hơn:

- thư mục runtime / generated
- data directories
- minified outputs
- artifact build như `zen.js`, `zen.min.js`

### 3.2. Repo có một nhóm file legacy không parse được bằng Prettier

Đây không phải bug mới do formatter tạo ra. Đây là code/HTML cũ vốn đã không hợp lệ theo parser hiện đại, nhưng trước đó chưa bị soi tới.

Những file này phải được exclude có chủ đích để đạt mục tiêu thực tế hơn:

> `npm run format` phải hoạt động ổn định trên phần còn lại của codebase, thay vì fail cả repo vì một nhóm file legacy đã biết.

Kết quả cuối cùng là:

- Prettier có thể chạy trên toàn repo theo nghĩa thực tế
- các file artifact/minified không bị rewrite vô ích
- các file legacy parse lỗi không còn chặn pipeline format

Và quan trọng hơn: format bây giờ là một command có thể dùng thật, không phải một nút trang trí trong `package.json`.

---

## 4. Test xanh lại sau một ngày dọn và sửa

Đây là phần quan trọng nhất.

Một ngày như hôm nay rất dễ rơi vào bẫy “dọn xong nhưng không chắc còn đúng”. Vì vậy điều có ý nghĩa không phải là đã sửa bao nhiêu file, mà là cuối ngày repo có còn giữ được hành vi không.

Kết quả cuối cùng: `npm test` xanh hết.

Điều đó có nghĩa là ba việc rất khác nhau đã cùng đứng vững sau thay đổi:

- test stabilization và cleanup trước đó không bị phá lại
- source tree mới sau khi flatten vẫn build đúng
- repo-wide formatting setup không làm vỡ luồng test/build

Nói ngắn gọn: đây không phải một ngày “trông có vẻ sạch hơn”. Đây là một ngày mà hệ thống chịu được thay đổi mạnh tay nhưng vẫn giữ được verification cuối cùng.

---

## 5. Nhìn lại

Hôm nay mệt đúng kiểu kỹ thuật.

Không có một bug lớn để đánh gục rồi ăn mừng. Có nhiều bug nhỏ, nhiều chỗ lệch môi trường, nhiều thứ legacy, nhiều dây nối cũ bị lộ ra khi đổi layout và khi chạy formatter trên toàn repo.

Nhưng cũng chính những ngày như vậy mới làm repo trưởng thành hơn.

Sau buổi sáng ngày 17:

- Windows bớt là một môi trường "ngoại lệ"
- `src/*` là cấu trúc thật của source tree
- `npm run format` là công cụ dùng được cho cả codebase
- `npm test` vẫn xanh

Không hào nhoáng. Nhưng rất đáng giá.

---

## Buổi chiều: Zig, WASM, và bài học về việc đo trước khi tối ưu

Đây là một trong những session thú vị nhất kể từ khi ZEN bắt đầu: ngày đầu tiên ZEN có một pipeline WASM thứ hai bên cạnh PEN.

Bắt đầu từ một quan sát đúng: toàn bộ các hàm crypto — SHA-256, Keccak-256, RIPEMD-160, HMAC, base62, ECC point multiply — đang chạy bằng BigInt thuần JavaScript. Với JavaScript thì điều này ổn, nhưng với `deterministicK` (vòng lặp RFC 6979) và `pointMultiply` (nhân điểm Weierstrass), nó trở nên chậm một cách rõ ràng dưới tải test.

Mục tiêu ban đầu: port toàn bộ sang Zig, compile ra WASM, thay thế từng file JS một.

### Những gì đã được xây dựng

9 module Zig trong session này:

- `src/sha256.zig` — streaming SHA-256 theo chuẩn FIPS 180-4
- `src/keccak256.zig` — Keccak-256 với 25 lane u64, thay thế BigInt state machine trong JS
- `src/ripemd160.zig` — RIPEMD-160 thuần, dùng cho BTC address generation
- `src/hmac_sha256.zig` — HMAC streaming, zero-alloc
- `src/base62.zig` — encode/decode u256/u512 với custom alphabet
- `src/curves/utils.zig` — toán Weierstrass đầy đủ: modPow, pointAdd, pointMultiply, RFC 6979
- `src/curves/secp256k1.zig` — curve params secp256k1
- `src/curves/p256.zig` — curve params P-256
- `src/crypto_wasm.zig` — entry point WASM: bump allocator 1MB, 20 export functions

Tất cả compile thành một file duy nhất: `crypto.wasm` — 66138 bytes.

Bridge JS: `src/crypto_wasm_bridge.js` — lazy loader, `bridge.ready` Promise, typed wrappers cho mọi export. Build script: `lib/build-crypto.js`. Pipeline ZEN: `buildPEN → buildCrypto → build-zen.js → minify`.

### Cú ngã và bài học thực tế

Sau khi wire toàn bộ JS hot path sang WASM — HMAC, deterministicK, pointMultiply — kết quả test gây sốc:

**Test suite chậm hơn 7–12 lần.**

- `sign() and encrypt() omit SEA prefix`: ~300ms → ~2191ms → **timeout** ở 2000ms mocha default
- Mọi test sign/verify: ~340ms → ~3800ms

Lý do:

| Hot path | Cách cũ | Cách mới | Kết quả |
|----------|---------|----------|---------|
| `hmacSha256` | WebCrypto `subtle.sign("HMAC")` — hardware SHA-NI | WASM software SHA-256 | WASM thua 7x |
| `deterministicK` | WebCrypto HMAC loop (RFC 6979) | WASM HMAC loop | WASM thua 7x |
| `pointMultiply` | V8 BigInt double-and-add | WASM u256×u256 emulated | WASM thua 8–12x |

Vấn đề cốt lõi:
- **WebCrypto HMAC** dùng hardware SHA-NI — CPU thực hiện SHA-256 ở tốc độ phần cứng
- **WASM32 không có wide multiply** — u256×u256 phải emulate bằng chuỗi 32-bit ops
- **V8 BigInt** là native C++ với hardware multiply — nhanh hơn WASM32 cho scalar multiply

### Quyết định cuối cùng

**Giữ WASM cho:**
- `keccak256` — 25-lane u64 state machine, WASM tránh hoàn toàn BigInt boxing overhead
- `ripemd160` — pure compute, không có hardware equivalent trong WebCrypto
- `base62` encode/decode — nhanh hơn BigInt cho encode path

**Revert về native cho:**
- `pointMultiply` → V8 BigInt double-and-add
- `hmacSha256` → WebCrypto `subtle.sign("HMAC", ...)` (hardware SHA-NI)
- `deterministicK` → WebCrypto HMAC loop (hardware SHA-NI)

### Kết quả

```
171 passing (28s)
```

| Test | Trước WASM | Sau WASM (sai) | Sau revert |
|------|-----------|---------------|-----------|
| sign + encrypt omit prefix | ~300ms | 2191ms (timeout) | **208ms** |
| signs deterministically | ~560ms | 6074ms | **566ms** |
| pair → encrypt → sign → verify | ~400ms | 3850ms | **406ms** |

### Bài học

> Điều quan trọng không phải là JS hay Zig — mà là cái gì nhanh hơn thì dùng cái đó.

1. **Đo trước khi tối ưu.** WASM không tự động nhanh hơn JS.
2. **WASM thắng khi:** pure compute BigInt-heavy không có WebCrypto equivalent (Keccak-256, RIPEMD-160).
3. **WASM thua khi:** platform đã có hardware acceleration (SHA-256, AES, HMAC).
4. **WASM32 thua BigInt cho scalar multiply** vì V8 BigInt là native C++, còn WASM32 phải emulate wide arithmetic.

Pipeline WASM vẫn ở trong repo — nền tảng để mở rộng khi có thuật toán phù hợp hơn (curve25519, BLS12-381, các hàm không có trong WebCrypto).

### Trạng thái repo cuối ngày 17

- `npm test` → **171 passing**
- `src/curves/utils.js` — dọn sạch dead code (bridge import, `_wasmReady`, `_bigIntTo32`, `_u8ToBigInt`, `isK1`)
- `crypto.wasm` — 66138 bytes, trong repo
- Build pipeline: `buildPEN → buildCrypto → build-zen.js → minifyZEN`